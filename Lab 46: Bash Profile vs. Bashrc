✅ Lab Completed: 46. Bash Profile vs. Bashrc
Repository Title Suggestion
Linux-Labs or Bash-Shell-Customization-Labs or Unix-Labs-Portfolio

README.md Content (Copy-Paste Ready)
Markdown# 46. Bash Profile vs. Bashrc

**Lab Completed** ✓

**Date Completed:** May 13, 2026

---

## 🎯 Objectives Achieved

- Understood the key differences between `.bash_profile` and `.bashrc`
- Learned how to customize the Bash shell environment
- Gained hands-on experience creating aliases and environment variables
- Mastered reloading configuration files without restarting the terminal

---

## 📋 Lab Tasks Completed

### 1. Examined Configuration Files
- Navigated to home directory (`cd ~`)
- Listed hidden files using `ls -a`
- Opened and reviewed both `~/.bash_profile` and `~/.bashrc`
- Understood their execution context:
  - **`.bash_profile`**: Runs for **login shells**
  - **`.bashrc`**: Runs for **interactive non-login shells**

### 2. Customized Shell Environment

**Backup Created (Best Practice)**
```bash
cp ~/.bashrc ~/.bashrc.backup
cp ~/.bash_profile ~/.bash_profile.backup
Added to ~/.bashrc:
Bash# Custom Aliases
alias ll='ls -la'           # Detailed list view
alias gs='git status'       # Quick git status
alias lt='ls --tree'        # Tree view (if supported)

# Environment Variable
export MY_VAR="Hello World!"
export PROJECT_PATH="$HOME/Projects"
Applied Changes
Bashsource ~/.bashrc
3. Verification

✅ ll command works as expected (shows detailed listing)
✅ echo $MY_VAR returns "Hello World!"
✅ New aliases and variables available in current and new terminal sessions


🛠️ Skills Demonstrated

Shell configuration management
Creating productive aliases
Managing environment variables
Understanding login vs non-login shells
Safe editing practices (backups)
